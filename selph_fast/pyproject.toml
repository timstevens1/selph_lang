[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "selph-fast"
version = "0.3.0"
description = "SELPH language — Python bindings for the Rust evaluator"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "selph_fast"
